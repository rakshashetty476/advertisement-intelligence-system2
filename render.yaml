services:
  - type: web
    name: advertisement-intelligence
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get update
      apt-get install -y tesseract-ocr
    startCommand: streamlit run app.py --server.port=$PORT --server.address=0.0.0.0
